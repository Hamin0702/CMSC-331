raw_scores = [{'student_name': 'John', 'raw_score': 8}, {'student_name': 'Mary', 'raw_score': 9}]  
final_grades = map(for x in raw_scores (lambda x: x[raw], raw_scores))

print(list(final_grades))

# Output:

#[80, 90]

